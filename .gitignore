node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.log
tasks.json
.env
.env.*

# Local agent tooling config, not part of the project
opencode.json

# Riaz protocol config (contains tokens) — never commit
.opencode/
