{
  "name": "agent-kanban-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix server test && node scripts/test-client-status.js && npm --prefix client run build",
    "start": "npm --prefix server start",
    "build": "npm --prefix client run build"
  }
}
